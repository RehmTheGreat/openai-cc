name: Apply Session 4.5 security finalization
on:
  push:
    branches: [agent/session-4-5-final-providers]
permissions:
  contents: write
jobs:
  apply:
    if: ${{ !contains(github.event.head_commit.message, '[session45-security-applied]') }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with: { fetch-depth: 0 }
      - uses: actions/setup-node@v4
        with: { node-version: 22 }
      - run: node scripts/session45-security-finalize.mjs
      - shell: bash
        run: |
          git config user.name "openai-cc-session45"
          git config user.email "actions@users.noreply.github.com"
          git add -A
          git commit -m "Harden Session 4.5 provider secret handling [session45-security-applied]"
          git push origin HEAD:${GITHUB_REF_NAME}
