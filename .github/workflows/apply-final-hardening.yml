name: Apply Final Hardening

on:
  push:
    branches:
      - agent/auth-admin-control-plane
  pull_request:

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/auth-admin-control-plane
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Apply deterministic transformation
        run: python scripts/final-hardening2.py
      - name: Verify transformed branch contents
        run: npm ci --no-audit --no-fund && npm test
      - name: Remove one-shot files and commit
        shell: bash
        run: |
          rm -f scripts/final-hardening2.py .github/workflows/apply-final-hardening.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Harden authentication error lifecycle"
          git push origin HEAD:agent/auth-admin-control-plane
